# Define if packages should always be updated
# Default: No
#  This can lead to config drift when ran on hosts already provisioned/configured
#  Use with caution
packages_update: no
packages_base:
  - vim-minimal
  - git
  - wget
